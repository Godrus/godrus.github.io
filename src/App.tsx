import React, { JSX } from "react";

const App: React.FC = (): JSX.Element => {
  return (
    <div className="App">
      <h1>Hello, React with TypeScript and SCSS!</h1>
    </div>
  );
};

export default App;