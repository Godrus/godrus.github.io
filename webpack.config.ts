/**
 * Главный файл конфигурации webpack
 * Перенаправляет на модульную конфигурацию в webpack-config/
 */
import { Configuration } from 'webpack';
import config from './webpack-config/index';

export default config;
